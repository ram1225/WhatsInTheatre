import { TruncateWordPipePipe } from './truncate-word-pipe.pipe';

describe('TruncateWordPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateWordPipePipe();
    expect(pipe).toBeTruthy();
  });
});
